version: '3.4'

services:
  hopkins.tech.server:
    container_name: ${DOCKER_REGISTRY-}hopkinstech
    build:
      context: .
      dockerfile: Server/Dockerfile
    working_dir: /app
    ports: 
        - "3000:443"