name: Continuous deployment

on:
  push:
    branches:
      - master

jobs:
  deployment:
    runs-on: self-hosted

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Start containers
        run: docker-compose up -d
